version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Example environment variables
      PYTHONUNBUFFERED: 1
      DEBUG: 'true'
    volumes:
      - ./backend:/app
    command: python main.py
